services:
  db:
    image: postgres:17-alpine
    restart: always
    ports:
      - "5432:5432"
    environment:
      - POSTGRES_USER=kanat
      - POSTGRES_PASSWORD=kanat
      - POSTGRES_DB=kanat
