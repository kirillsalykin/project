apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
namespace: default
resources:
  - db.yaml
  - deployment.yaml
  - service.yaml
images:
  - name: docker.io/kirillsalykin/project
    newTag: "back-5" # {"$imagepolicy": "flux-system:back:tag"}
configMapGenerator:
  - name: config
    files:
      - config.yaml
