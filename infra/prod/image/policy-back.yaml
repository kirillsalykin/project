---
apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: back
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: default
  filterTags:
    pattern: 'back-^\d+$'
  policy:
    numerical:
      order: asc
