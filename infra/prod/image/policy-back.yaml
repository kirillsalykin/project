---
apiVersion: image.toolkit.fluxcd.io/v1beta1
kind: ImagePolicy
metadata:
  name: back
  namespace: flux-system
spec:
  imageRepositoryRef:
    name: default
  filterTags:
    pattern: '^back-(?P<ver>[0-9]+)$'
    extract: '$ver'
  policy:
    numerical:
      order: asc
